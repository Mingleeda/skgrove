# SK Grove 톤앤매너 · 디자인 기준

이 문서는 **결정과 그 이유**를 적는다. 값만 옮겨 적으면 다음 사람이 왜 그런지 몰라
다시 흔든다. 값의 최종 출처는 언제나 `src/styles.css`의 `:root` 블록이고, 이 문서와
어긋나면 코드가 맞다.

---

## 1. 이 앱이 무엇인가

대나무숲(익명 의견) → 안건 → 익명 투표 → 액션아이템으로 이어지는 사내 팀문화 웹앱.
곁들여 팀 놀이 기능(커피뽑기·조뽑기·유머게시판·팀추억)이 붙는다.

**심리적 안전이 제품의 전제**다. 익명으로 말한 것이 어디로 가는지 보이고, 개인이
낙인찍히지 않아야 한다. 이 전제가 디자인 판단 여러 곳에서 근거로 쓰인다.

---

## 2. 색 — Moss & Clay

브랜드색이 **표면을 점유하지 않는다.** 예전에는 사이드바와 히어로 전체가 딥그린
`#17352f` 이었는데, 표면을 차지한 색은 나이가 빨리 든다. 지금은 모래빛 중립면 위에
이끼색을 액센트로만 쓴다.

### 표면 (3단)

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--color-page` | `#f6f4ef` | 사이드바 · 앱 배경 |
| `--color-surface` | `#fffdf9` | 카드 · 패널 |
| `--color-sunken` | `#efece4` | 호버 · 입력 · 진행바 트랙 |

### 글자 — 위계는 색이 아니라 명도로 만든다

| 토큰 | 값 | 대비(surface) |
|---|---|---|
| `--color-ink` | `#1f2420` | 15.5:1 |
| `--color-muted` | `#5a5f56` | 6.5:1 |

### 역할색 — 세 표면 모두에서 AA(4.5:1) 이상

| 토큰 | 값 | 의미 |
|---|---|---|
| `--color-moss` / `--color-moss-strong` | `#4f7350` / `#3c5a3d` | 브랜드 · 찬성 · 통과 |
| `--color-clay` | `#96502f` | 문화면 보조 · 강조 |
| `--color-danger` | `#a33a3a` | 반대 · 부결 |
| `--color-pending` | `#77642f` | 투표중 · 대기 |
| `--color-info` | `#39628f` | 링크 · 정보 |

`--color-info`는 나중에 추가됐다. 파랑이 68곳에 쓰이는데 팔레트에 없었고, 기존
`#2f74c0`은 `--color-sunken` 위에서 4.06:1로 AA 미달이었다.

### 틴트 짝 — 배지 배경과 글자색은 항상 세트

`--tint-moss` / `--tint-moss-ink` 처럼 짝으로만 쓴다(AAA 7:1 이상). **틴트 배경에
역할색을 얹지 않는다** — 실제로 `.status-pill.action-*` 세 규칙이 이걸 어겨 대비가
떨어져 있었다.

| 배경 | 글자 |
|---|---|
| `--tint-moss` `#e8efe6` | `--tint-moss-ink` `#2f4a30` |
| `--tint-clay` `#f5e9e1` | `--tint-clay-ink` `#5c3018` |
| `--tint-danger` `#f6e5e5` | `--tint-danger-ink` `#6b2020` |
| `--tint-pending` `#f2eddc` | `--tint-pending-ink` `#4a3d15` |
| `--tint-info` `#e4ecf4` | `--tint-info-ink` `#1f3f5e` |

### 경계선

`--color-border` `#e2ddd1` / `--color-border-strong` `#cfc8b8`

**카드 분리는 그림자가 아니라 경계선으로 한다.** 그림자는 떠 있는 것(토스트·팝오버)
에만 쓰고, 그때도 `--shadow-float` 하나만 쓴다.

---

## 3. 타이포그래피

폰트는 **Pretendard Variable** 고정(`main.tsx`에서 실제 로드). 이 앱은 전부
한국어인데 예전 스택에 한국어 폰트가 없어 기기마다 서체가 달랐다.

### 스케일 — 7단계

예전에는 서로 다른 크기가 21종, `line-height`가 19종이었다. 13px와 14px, 1.5와
1.55는 눈으로 구분되지 않으면서 리듬만 깬다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--text-micro` | 11px | 보조 메타 |
| `--text-meta` | 12px | 날짜 · 라벨 |
| `--text-body` | 13px | 본문 |
| `--text-body-lg` | 15px | 강조 본문 · 패널 제목 |
| `--text-title` | 18px | 섹션 제목 |
| `--text-heading` | 22px | 화면 제목 |
| `--text-display` | 28px | 문화면 히어로 |

`--leading-tight` 1.35 / `--leading-normal` 1.5 / `--leading-relaxed` 1.7

### 굵기 — 700 / 600 / 500 만 쓴다

800·900을 쓰지 않는다. 예전에 900이 37건 있었는데 그중 33건이 10~13px 작은 글자였고,
**Pretendard 900은 한글 작은 크기에서 획이 뭉갠다.**

### 한국어 줄바꿈 — 반드시 짝으로

```css
word-break: keep-all;
overflow-wrap: anywhere;
```

`keep-all`만 넣으면 어절은 지켜지지만 긴 URL이 컨테이너를 가로로 뚫는다. 제목에는
`text-wrap: balance`, 숫자에는 `font-variant-numeric: tabular-nums`.

---

## 4. 형태 · 간격 · 모션

| 종류 | 토큰 |
|---|---|
| 라운드 | `--radius-sm` 6px(배지·입력) / `--radius` 10px(버튼·작은 카드) / `--radius-lg` 14px(카드·패널) / `--radius-full` 999px |
| 간격 | `--space-1` 4 · `-2` 8 · `-3` 12 · `-4` 16 · `-5` 20 · `-6` 24 · `-8` 32 · `-10` 40 · `-12` 48 |
| 밀도 | `--row-h` 44px — 업무면 행 높이. 화면마다 다르면 눈이 다시 적응해야 한다 |
| 모션 | `--ease-ui` 150ms ease-out / `--ease-enter` 280ms cubic-bezier(0.22, 1, 0.36, 1) |
| 그림자 | `--shadow-float` — 떠 있는 것에만 |

전이는 `:where(...)` 목록 하나로 핵심 컨트롤 전체에 걸려 있다(`styles.css` "상태 레이어"
주석 참조). 새 컨트롤을 만들면 그 목록에 추가한다.

---

## 5. 화면 문법 — 업무면 / 문화면

화면마다 임의로 정하지 않도록 판정 기준을 고정한다.

| 문법 | 판정 | 해당 화면 |
|---|---|---|
| **업무면** | 목록을 **훑고 비교**하는 화면 | 안건함, 액션아이템, 리더 관리함, 알림, 계정 관리, 파트지수 |
| **문화면** | 하나를 **읽거나 쓰는** 화면 | 대나무숲 접수, 로그인, 팀 추억, 유머게시판, 동료 성향, 커피뽑기 |
| **포스터면** | 여러 개를 **눈으로 훑는** 화면 | 번개 모임, 일정 공모 |
| 혼합 | 위가 문화면, 아래가 업무면 | 홈, 캔미팅 |

**업무면** — `.work-list` / `.work-row`. 44px 고정 행, 그림자 없이 경계선으로 분리,
상태는 7px 색점, 집계 퍼센트는 `--color-ink` 600 굵기 + `tabular-nums`.
행은 높이가 고정이라 긴 한글 제목이 잘리므로 `title` 속성으로 전문을 남긴다.

**문화면** — `.culture-display`(28px) / `.culture-lede` / `.culture-mark`(틴트 강조)
/ `.culture-avatar`(틴트 배경 + 이니셜, 겹침 배치).

**포스터면** — `.poster-grid` / `.poster-cell` / `.poster`(4:5 고정).
업무면은 글자를 읽게 하고, 포스터면은 **보게** 한다. 격자를 훑다가 눈에 걸리는 것을
누르는 화면이라 개별 카드의 화려함보다 **격자가 흐트러지지 않는 것**이 우선이다.

세 가지가 이 원칙에서 나온다.

1. **틀은 고정, 바뀌는 것은 색·아이콘·문구뿐.** 비율(4:5)·타이포·여백은 CSS 가
   쥐고, `--tint-*` 네 가지와 lucide 아이콘 12개 안에서만 고른다.
   사진 생성 모델을 쓰지 않는 이유가 이것이다 — 건마다 화풍이 달라 스무 장이
   쌓이면 격자가 무너진다.
2. **문구는 2줄에서 자른다**(`-webkit-line-clamp`). 제목 입력은 40자까지
   허용하는데, 좁은 격자(171px)에서 그 길이는 네 줄이 되어 포스터 밖으로 밀린다.
   말줄임표가 붙으므로 잘렸다는 사실도 보인다.
3. **사진 위에는 반드시 스크림.** 첨부 사진의 밝기를 알 수 없으므로
   `--scrim-strong` 그라디언트를 깔고 그 위에 흰 글자를 얹는다.

### 행으로 바꾸지 않는 목록

`.action-card`와 `.issue-card`는 담당자 select·상태 select·메모 편집기를 품고 있다.
44px 행에 넣으면 그 컨트롤을 없애거나 새 펼침 동작을 만들어야 한다.
**훑고 고르는 목록과 손대는 목록은 같은 문법을 쓰지 않는다.**

---

## 6. 컴포넌트 어휘

| 클래스 | 용도 |
|---|---|
| `.btn-primary` | 주 액션(이끼 채움) |
| `.btn-secondary` | 보조(가라앉은 면) |
| `.btn-ghost` | 이동·취소처럼 화면을 떠나는 동작(면 없음) |
| `.badge` + `.badge-moss/clay/danger/pending/info` | 상태 배지. 틴트 짝 사용 |
| `EmptyState` | 아이콘 + 제목 + 설명 + 다음 행동 |

### 상태 → 배지 대응 (화면마다 다르게 정하지 않는다)

| 상태 | 클래스 |
|---|---|
| 통과 · 완료 · 찬성 | `badge-moss` |
| 부결 · 반려 · 반대 | `badge-danger` |
| 투표중 · 진행중 · 대기 | `badge-pending` |
| 정보 · 참고 | `badge-info` |
| 문화면 강조 | `badge-clay` |

### 액션아이템 상태 아이콘

대기 `Clock` · 진행중 `CirclePlay` · 완료 `CircleCheck` · 재검토 `RotateCcw`.
배지와 전환 버튼이 **같은 상태에 같은 아이콘**을 쓴다.
정의는 `features/actions/actionDisplay.ts` 한 곳에 있고 홈 요약도 이걸 쓴다 —
같은 데이터를 두 화면이 다르게 말하면 사용자가 어느 쪽을 믿을지 판단해야 한다.

### 모임 상태 → 포스터 배지

| 상태 | 배지 톤 | 판정 |
|---|---|---|
| 모집중 | `moss` | 자리가 남고 마감 전 |
| 마감 | `clay` | 정원이 찼거나 마감 시각이 지남 |
| 진행함 · 종료 · 취소 | `muted` | 지난 일 — 눈에 걸리지 않게 가라앉힌다 |

지연·대기처럼 **주의를 끌되 실패는 아닌 것**에 `clay` 를 쓴다.
`danger` 는 부결·삭제처럼 되돌릴 수 없는 것에만 남겨둔다.

---

## 7. 반응형

브레이크포인트는 **720px / 1100px 두 개뿐**이다(테스트가 강제).

- **≤1100px** — 사이드바가 아이콘만 남기고 좁아짐
- **≤720px** — 사이드바가 **하단 탭바**로 전환(가로 스크롤, 10px 라벨,
  `env(safe-area-inset-bottom)` 반영). 버튼 3단 최소 높이 40px.
  **자르지 말고 줄바꿈한다** — 모바일에는 호버가 없어 `title` 툴팁이 무용하다.

### CSS 순서 함정 (이 저장소에서 반복해서 밟았다)

모바일 오버라이드는 **기본 규칙보다 뒤에** 와야 한다. 같은 특이도에서는 순서가
이긴다. `.work-row`·`.overview-bar` 기본 규칙이 파일 뒤쪽에 있어, 앞선 미디어쿼리에
넣으면 조용히 진다. **계산된 스타일을 확인하기 전에는 "적용했다"고 말할 수 없다.**

---

## 8. 카피 원칙

- **제품을 설명하는 말과 사용자에게 하는 말이 겹치면 설명 쪽을 버린다.**
  로그인 헤드라인이 무엇을 하는 앱인지 말하므로 "팀문화 개선 웹앱" 부제는 뺐다.
- **상시 노출 설명문은 모든 방문에 세금을 매긴다.** 답은 질문이 생기는 자리에서 한다.
  익명 접수가 목록에 없어 당황하는 순간, 빈 상태가 이미 "실명으로 접수한 의견이
  없어요"라고 말하고 익명 조회가 바로 아래 있다.
- **비활성 버튼에는 사유를 적는다.** 흐리게만 처리하면 사용자는 고장으로 읽는다.
- **필수 표시는 별표가 아니라 '필수' 낱말로.** 별표는 스크린리더가 "별"로 읽고,
  색만으로 나르면 색각 이상에서 사라진다.
- **완곡어법이 뜻을 숨기면 양쪽 모두에게 실패한다.** "새싹 감각"은 모르는 사람은
  못 알아듣고 아는 사람은 나이 구간임을 간파한다. 라벨 옆에 실제 구간을 적는다.

---

## 9. 아이콘과 이모지

**인터페이스 기호는 lucide 아이콘**을 쓴다. 이모지는 OS마다 다른 그림으로 그려진다 —
한국어 폰트를 Pretendard로 고정한 것과 같은 문제다. 스크린리더가 문장 중간에
"파티 크래커"를 읽는 문제도 있다.

**이모지를 남기는 곳 두 군데:**
- 사용자가 고르는 **반응**(팀 추억의 👍👏😂🔥💚) — Slack·Notion·GitHub도 그렇게 한다
- **슬랙으로 나가는 메시지** — 그쪽에서는 관용적이다

---

## 10. 접근성 기준선

두 역할(팀리더 / 팀원) × 두 폭(1280px / 390px)에서 **결함 0**을 유지한다.

| 항목 | 기준 |
|---|---|
| 텍스트 대비 | AA(4.5:1), 큰 글자 3:1 — **렌더된 결과로** 측정 |
| 클릭 타깃 | 데스크톱 32px / 모바일 36px 이상 |
| 폼 컨트롤 | 모두 이름이 있어야 함(`<label>` 또는 `aria-label`) |
| 잘림 | `ellipsis`면 `title`로 전문을 남기거나, 모바일은 줄바꿈 |
| 가로 넘침 | 0 |

`src/designTokens.test.ts`가 **정적으로** 강제하는 것: 역할색 AA, 틴트 짝 AAA,
하드코딩 색상값 0, 정의 없는 `var()` 0, 브레이크포인트 2종, 한국어 줄바꿈 규칙.

**단, 이 테스트가 잡지 못하는 것이 더 많다.** 실제로 발견한 결함 대부분은 정적
검사를 통과한 상태에서 브라우저 대비 계산으로만 드러났다. 토큰 정의가 옳은 것과
그 토큰을 옳게 쓰는 것은 다른 문제다.

---

## 11. 아직 정하지 못한 것

- **시각적 서명이 없다.** moss 계열이 319회로 색 운용은 건강하지만, 라임을 버린 뒤
  "SK Grove답다"고 할 요소가 생기지 않았다. 색 조정이 아니라 하나의 기억할 만한
  요소(일러스트·마스코트·독특한 그래프 스타일)가 필요한 브랜드 결정이다.
- **메뉴 5개가 슬래시 복합명이다.** `안건함 / 투표` 등. 팀이 이미 쓰는 말이라 합의가
  필요하다.
- **용어**: `접수`(관공서 어감) · `관리함` · `파트지수`(무엇의 지수인지 없음).
